<!DOCTYPE HTML>
<html lang="pt-br">
	<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Multiroom Chat</title>
    <link rel="shortcut icon" href="images/chat-icon.png" sizes="32x32" type="image/png">
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
		<link href="css/style.css" rel="stylesheet">
	</head>

	<body>

		<div class="container">
      <div class="row vertical-offset-80">
        <div class="col-md-4 col-md-offset-4">
          <div class="panel">
            <div class="panel-heading">                                
              <div class="row-fluid user-row">
                <img src="images/login.gif" class="img-responsive" alt="Multiroom Chat"/>
              </div>
            </div>
            <div class="panel-body">
              <h3 class="bg-warning">Chat Multiroom</h3>
              <form accept-charset="UTF-8" action="/chat" method="POST">
                <fieldset>
                  <label class="panel-login">
                    <div class="login_result"></div>
                  </label>
                  <input type="text" class="form-control" placeholder="Nome ou apelido" name="apelido">
                  <br></br>
                  <input class="btn btn-lg btn-success btn-block" type="submit" id="entrar" value="Entrar »">
                </fieldset>
              </form>
            </div>
          </div>

          <div>
            <% if(validacao.length > 0) { %>
              <div class="alert alert-danger">
                <h6 class="text-muted text-center">ERROS!</h6>
                <hr>
                <div>
                  <% for(let i=0; i<validacao.length; i++) { %>
                    <p>- <%=validacao[i].msg%></p>
                  <% } %>
                </div>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
		
	</body>
</html>