<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Cadastro de Notícia</title>
    <link rel="shortcut icon" href="images/node-icon.png" sizes="32x32" type="image/png" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" />
  </head>
  
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">
        <img src="images/logo.png" class="d-inline-block align-top" alt="">
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navBar" aria-controls="navBar" aria-expanded="false" aria-label="Alterna navegação">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navBar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/noticias">Notícias</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/formulario">Cadastrar Notícias</a>
          </li>
        </ul>
      </div>
    </nav>
    
    <div class="container-fluid">
      <div class="row justify-content-center">
        <div class="col-md-5">
          <div class="card mt-5 bg-light">
            <% if(validacao.length > 0) { %>
              <div class="alert alert-danger m-2">
                <p class="text-small">
                  <% for(let i=0; i<validacao.length; i++) { %>
                    <span>-
                      <%= validacao[i].msg %>
                    </span> <br>
                  <% } %>
                </p>
              </div>
            <% } %>
            <div class="card-body">
              <form action="/noticias/salvar" method="post">
                <div class="form-group">
                  <label class="font-weight-bold" for="titulo">Título *</label>
                  <input type="text" class="form-control" value="<%= noticia.titulo%>" name="titulo">
                </div>
                <div class="row">
                  <div class="col">
                    <label class="font-weight-bold" for="autor">Autor *</label>
                    <input type="text" class="form-control" value="<%= noticia.autor%>" name="autor">
                  </div>
                  <div class="col">
                    <label class="font-weight-bold" for="dia_ocorrido">Data do Ocorrido</label>
                    <input type="date" class="form-control" value="<%= noticia.dia_ocorrido%>" name="dia_ocorrido">
                  </div>
                </div>
                <div class="form-group mt-3">
                  <label class="font-weight-bold" for="noticia">Inserir notícia *</label>
                  <textarea class="form-control" name="noticia" rows="3"><%= noticia.noticia%></textarea>
                </div>
                <button type="submit" class="btn btn-primary float-right">Enviar</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>

  </body>    
</html>
